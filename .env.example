# Shopify App Credentials (from Shopify Partner Dashboard)
SHOPIFY_API_KEY=your_api_key_here
SHOPIFY_API_SECRET=your_api_secret_here

# App Configuration
APP_URL=https://your-app-url.ngrok.io
PORT=3000
NODE_ENV=development

# Session Secret (generate a random string with: openssl rand -base64 32)
SESSION_SECRET=your_random_secret_here

# Database (SQLite for dev, PostgreSQL for production)
# For local dev: sqlite:sessions.db
# For production: postgresql://user:pass@host:5432/dbname
DATABASE_URL=sqlite:sessions.db

# Scopes needed for the app
SCOPES=read_orders,read_products
